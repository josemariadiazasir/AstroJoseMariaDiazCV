---
const personalImageAlt = "José María Díaz Moreno";
import Badge from "./Badge.astro";
import LinkedInIcon from "./icons/LinkedIn.astro";
import MailIcon from "./icons/Mail.astro";
import GitHubIcon from "./icons/GitHub.astro";
import SocialPill from "./SocialPill.astro";
import { Image } from 'astro:assets';
import profileImg from '../assets/profile.webp';
---

<div class="max-w-xl mx-auto hero-container">
  <div class="flex flex-col sm:flex-row gap-4 mb-6 items-center">
    <!-- Imagen de perfil -->
    <div class="rounded-full w-24 h-24 overflow-hidden shadow-lg ring-2 ring-[var(--red)] ring-offset-2 ring-offset-transparent">
      <Image
        class="object-cover w-full h-full"
        style={{ objectPosition: "50% 20%" }}
        src={profileImg}
        alt={personalImageAlt}
        width={96}
        height={96}
        loading="eager"
      />
    </div>

    <a
      href="/cv-jose-maria-diaz-moreno.pdf"
      download
      class="mt-4 sm:mt-0 flex items-center transition md:justify-center md:hover:scale-105"
    >
      <Badge>Descargar CV</Badge>
    </a>
  </div>

  <!-- Título principal -->
  <h1 class="text-center sm:text-left text-5xl sm:text-6xl font-bold tracking-tight text-[var(--text-color-dark)] dark:text-[var(--text-color-light)] leading-tight">
    Hola, soy <span class="text-[var(--red)]">José María</span>
  </h1>

  <!-- Descripción -->
  <p class="mt-6 text-center sm:text-left text-xl text-[var(--text-color-dark)] dark:text-gray-300 [&>strong]:text-[var(--red)] [&>strong]:font-semibold leading-relaxed">
    Ingeniero informático y <strong>software engineer</strong> especializado en <strong>React y TypeScript</strong>, con experiencia construyendo aplicaciones web en Madrid.
  </p>

  <!-- Stats -->
  <div class="flex gap-8 mt-8 justify-center sm:justify-start">
    <div class="text-center sm:text-left">
      <span class="block text-3xl font-bold text-[var(--red)]">+5</span>
      <span class="text-sm text-gray-500 dark:text-gray-400">años de experiencia</span>
    </div>
    <div class="text-center sm:text-left">
      <span class="block text-3xl font-bold text-[var(--red)]">5</span>
      <span class="text-sm text-gray-500 dark:text-gray-400">empresas</span>
    </div>
    <div class="text-center sm:text-left">
      <span class="block text-3xl font-bold text-[var(--red)]">React</span>
      <span class="text-sm text-gray-500 dark:text-gray-400">stack principal</span>
    </div>
  </div>

  <!-- Navegación social -->
  <nav class="flex flex-col sm:flex-row gap-4 mt-8 justify-center sm:justify-start">
    <SocialPill href="mailto:josediazasir@gmail.com">
      <MailIcon class="size-4" />
      Contáctame
    </SocialPill>
    <SocialPill href="https://www.linkedin.com/in/jose-maria-diaz-moreno-292b671b0/">
      <LinkedInIcon class="size-4" />
      LinkedIn
    </SocialPill>
    <SocialPill href="https://github.com/josemariadiazasir">
      <GitHubIcon class="size-4" />
      GitHub
    </SocialPill>
  </nav>
</div>

<style>
  .hero-container {
    animation: fadeInUp 0.6s ease-out both;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(24px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-container {
      animation: none;
    }
  }
</style>
